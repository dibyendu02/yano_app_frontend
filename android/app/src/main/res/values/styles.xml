<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
        <item name="android:timePickerDialogTheme">@style/Dialog.Theme</item>
    </style>
    
    <!-- Configuration for Dialog.theme. -->
    <style name="Dialog.Theme" parent="Theme.AppCompat.Light.Dialog">
        <!-- Accent color, might influence the clock hands or selected time color -->
        <item name="colorAccent">#00263E</item>
        <!-- Primary text color, used for the time text -->
        <item name="android:textColorPrimary">#000000</item>
        <!-- <item name="android:textColorSecondary">#00263E</item> -->
        
    </style>

</resources>
